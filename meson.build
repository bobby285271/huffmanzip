project('huffmanzip', ['cpp', 'c'], 
  version: '0.1.0',
  meson_version: '>= 0.59.0',
  default_options: [ 
    'warning_level=2',
    'cpp_std=gnu++20',
  ],
)

i18n = import('i18n')

subdir('data')
subdir('src')
subdir('po')
